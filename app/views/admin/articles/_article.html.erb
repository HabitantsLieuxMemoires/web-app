<tr>
  <td><%= article.title %></td>
  <td><%= article.modifier.nickname %></td>
  <td>
    <%= link_to t('delete'), unfeature_admin_feature_article_path(article.feature.id, article.id), class: 'btn btn-danger btn-sm' %>
  </td>
</tr>
