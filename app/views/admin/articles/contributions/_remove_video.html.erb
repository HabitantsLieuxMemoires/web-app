<td><span class="label label-danger"><%= t('article.video.track.removed') %></span></td>
<td>
  <%= link_to admin_article_video_path(contribution.article_id, contribution.video_id),
          class: "btn btn-default btn-xs",
          data: {
            toggle: 'modal',
            target: '#modal-view-video'
          }  do %>
      <%= fa_icon 'eye', text: t('article.video.do.view'), right: true %>
  <% end %>
  <%= link_to restore_admin_article_video_path(contribution.article_id, contribution.video_id), class: "btn btn-primary btn-xs" do %>
      <%= fa_icon 'undo', text: t('article.video.do.restore'), right: true %>
  <% end %>
</td>
