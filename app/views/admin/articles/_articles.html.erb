<table class="table table-hover">
  <tbody id="featured-articles">
    <tr>
      <th class="col-md-5"><%= t('models.article.title') %></th>
      <th class="col-md-5"><%= t('models.article.author') %></th>
      <th class="col-md-2"><%= t('actions') %></th>
    </tr>
    <tr>
      <td>
        <div class="input-group">
          <input class="form-control select-article-url" type="text" placeholder="<%= t('models.article.search') %>">
          <span class="input-group-btn">
            <button class="btn btn-default" id="add-article" type="button">
              <%= t('add') %>
            </button>
          </span>
        </div>
      </td>
      <td></td>
      <td></td>
    </tr>

    <%= render partial: 'admin/articles/article', collection: articles %>

  </tbody>
</table>
