<%= render 'header' %>

<div class="row">
  <div class="col-md-12">
    <ul class="timeline">
      <%= render_activities(@activities) %>
    </ul>

    <div class="text-center">
      <%= paginate @activities, :theme => 'twitter-bootstrap-3' %>
    </div>
  </div>
</div>
