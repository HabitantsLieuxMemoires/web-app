<h1>Reset password</h1>

<%= form_tag password_resets_path, :method => :post do %>
    <div class="field">
      <%= label_tag :identity %><br />
      <%= text_field_tag :identity %>
    </div>
    <div class="actions">
      <%= submit_tag "Reset password" %>
    </div>
<% end %>