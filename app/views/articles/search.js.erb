var results   = $('#search-results');
var noResults = $('#no-result');

// Emptying first
results.empty();

<% if @articles.any? %>
  noResults.addClass('hidden');
  $('<%= j(render "articles/search_results", articles: @articles) %>').appendTo(results);
<% else %>
  noResults.removeClass('hidden');
<% end %>
