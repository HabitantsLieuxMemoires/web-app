<%= render 'shared/sidebar_actions' %>

<%= render_cell :themes,        :index %>

<% if content_for?(:last_cell) %>
  <%= yield(:last_cell) %>
<% else %>
  <%= render_cell :news, :index %>
<% end %>

<%= render_cell :chronologies,  :index %>
