<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-hlm-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'HLM', root_url, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse navbar-hlm-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
            <li><%= t('logged_in_as') %> <%= current_user.email %></li>
            <li><%= link_to t('logout'), logout_path %></li>
        <% else %>
            <li><%= link_to t('signup'), signup_path %></li>
            <li><%= link_to t('login'), login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<div class="business-header">
  <div class="container">
  </div>
</div>